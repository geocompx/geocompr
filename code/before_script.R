library(methods)
library(rgeobook)

knitr::opts_chunk$set(
        comment = "#>",
        collapse = TRUE,
        cache = TRUE,
        fig.align="center",
        fig.pos="t"
)

set.seed(2017)
options(digits = 3)
options(dplyr.print_min = 4, dplyr.print_max = 4)

rgeobook:::generate_chapter_code()
