# Basic map making {#basic-map}

As with **sp**, basic maps are created by an extension of the `plot()` function. Unlike **sp**'s implementation of this method to spatial data, however, **sf** creates a faceted plot of multi-column objects:

```{r}
plot(w)
```


<!-- 
- plot() function 
- map export 
-->